BASEDIR = $(shell pwd)
REBAR = rebar3
LIBPATH = $(BASEDIR)/_build/default/lib/{{ name }}

.PHONY: compile clean test shell

compile:
	$(REBAR) compile

clean:
	$(REBAR) clean

test:
	$(REBAR) ct

shell:
	$(BASEDIR)/_build/default/plugins/efene/fnshell -pz $(LIBPATH)/ebin/
